// in this instead of returning indexes we need to return yes or no or true or false if a two sum exists in array
//its most optimal approach would be two pointers , whereas its brute force is same as noraml two sumwe have done before
#include<bits/stdc++.h>